# Vendored GitHub Star Chart Action

This local action is synchronized from the private development repository
`3441293738/github-star-chart` at commit `a1ca0d0`.

It is vendored here because GitHub only permits sharing a private action with
other private repositories. CreatorHub is public, so a local action avoids a
personal access token and keeps the workflow self-contained.
